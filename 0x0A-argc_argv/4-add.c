#include <stdio.h>
#include <stdlib.h>

/**
 * main - program that adds positive numbers.
 * @argc: arguments passed to the program
 * @argv: array containing arguments
 *
 *
 * Return: result that adds positive numbers
 */

int main(int argc, char *argv[])
{
	int sum, i;

	if (no number)
	{
		printf("0\n");
		return (0);
	}
	if (number with no digit)
	{
		printf("Error");
		return (1);
	}
	sum += atoi(argv[i]);
	printf("", );
}
